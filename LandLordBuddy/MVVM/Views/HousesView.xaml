<UserControl x:Class="LandLordBuddy.MVVM.Views.HousesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:LandLordBuddy.MVVM.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <UserControl.Resources>
        <converters:IsNotNullConverter x:Key="IsNotNullConverter"/>
    </UserControl.Resources>
    <StackPanel Margin="20">
        <TextBlock Text="Houses" FontSize="28" FontWeight="Bold" Margin="0,0,0,20"/>
        
        <!-- Search and Filters -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <TextBox Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}" 
                     Width="300" Margin="0,0,10,0" 
                     Padding="10,5"/>
            <CheckBox Content="Active Only" IsChecked="{Binding ShowActiveOnly}" 
                     VerticalAlignment="Center" Margin="0,0,10,0"/>
            <Button Content="Add House" Command="{Binding AddHouseCommand}" 
                   Padding="15,8" Margin="0,0,10,0"/>
            <Button Content="Remove House" Command="{Binding RemoveHouseCommand}" 
                   Padding="15,8" Margin="0,0,10,0"
                   Background="#F44336"
                   Foreground="White"
                   BorderThickness="0">
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedHouse, Converter={StaticResource IsNotNullConverter}}" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                                <Setter Property="IsEnabled" Value="False"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Button Content="Export Report" Command="{Binding ExportReportCommand}" 
                   Padding="15,8"/>
        </StackPanel>

        <!-- Houses List -->
        <DataGrid ItemsSource="{Binding Houses}" 
                 SelectedItem="{Binding SelectedHouse}"
                 AutoGenerateColumns="False" 
                 IsReadOnly="True"
                 GridLinesVisibility="All"
                 HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Address" Binding="{Binding AddressLine1}" Width="*"/>
                <DataGridTextColumn Header="City" Binding="{Binding City}" Width="120"/>
                <DataGridTextColumn Header="Postcode" Binding="{Binding Postcode}" Width="100"/>
                <DataGridTextColumn Header="Rooms" Binding="{Binding TotalRooms}" Width="80"/>
                <DataGridTextColumn Header="Active Tenants" Binding="{Binding ActiveTenantCount}" Width="120"/>
                <DataGridTextColumn Header="Monthly Rent" Width="120">
                    <DataGridTextColumn.Binding>
                        <Binding Path="TotalMonthlyRent" StringFormat="£{0:N2}"/>
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</UserControl>
