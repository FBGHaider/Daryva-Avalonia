<UserControl x:Class="LandLordBuddy.MVVM.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <ScrollViewer>
        <StackPanel Margin="20">
            <TextBlock Text="Dashboard" FontSize="28" FontWeight="Bold" Margin="0,0,0,20"/>
            
            <!-- KPI Cards -->
            <UniformGrid Columns="5" Margin="0,0,0,20">
                <Border Background="#E3F2FD" Padding="20" Margin="5" CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="Houses" FontSize="14" Foreground="#666"/>
                        <TextBlock Text="{Binding HousesCount}" FontSize="32" FontWeight="Bold"/>
                    </StackPanel>
                </Border>
                <Border Background="#E8F5E9" Padding="20" Margin="5" CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="Active Tenants" FontSize="14" Foreground="#666"/>
                        <TextBlock Text="{Binding ActiveTenantsCount}" FontSize="32" FontWeight="Bold"/>
                    </StackPanel>
                </Border>
                <Border Background="#FFF3E0" Padding="20" Margin="5" CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="Rent Due This Month" FontSize="14" Foreground="#666"/>
                        <TextBlock FontSize="32" FontWeight="Bold">
                            <TextBlock.Text>
                                <Binding Path="RentDueThisMonth" StringFormat="£{0:N2}"/>
                            </TextBlock.Text>
                        </TextBlock>
                    </StackPanel>
                </Border>
                <Border Background="#FFEBEE" Padding="20" Margin="5" CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="Overdue Rent" FontSize="14" Foreground="#666"/>
                        <TextBlock Text="{Binding OverdueRentCount}" FontSize="32" FontWeight="Bold"/>
                        <TextBlock FontSize="14" Foreground="#666">
                            <TextBlock.Text>
                                <Binding Path="OverdueRentAmount" StringFormat="£{0:N2}"/>
                            </TextBlock.Text>
                        </TextBlock>
                    </StackPanel>
                </Border>
                <Border Background="#F3E5F5" Padding="20" Margin="5" CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="Documents Expiring" FontSize="14" Foreground="#666"/>
                        <TextBlock Text="{Binding DocumentsExpiringSoonCount}" FontSize="32" FontWeight="Bold"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <!-- Quick Actions -->
            <TextBlock Text="Quick Actions" FontSize="20" FontWeight="Bold" Margin="0,20,0,10"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <Button Content="Add House" Command="{Binding AddHouseCommand}" Margin="5" Padding="15,8"/>
                <Button Content="Add Tenant" Command="{Binding AddTenantCommand}" Margin="5" Padding="15,8"/>
                <Button Content="Record Payment" Command="{Binding RecordPaymentCommand}" Margin="5" Padding="15,8"/>
                <Button Content="Upload Document" Command="{Binding UploadDocumentCommand}" Margin="5" Padding="15,8"/>
            </StackPanel>

            <!-- Rent Due in Next 7 Days -->
            <TextBlock Text="Rent Due in Next 7 Days" FontSize="20" FontWeight="Bold" Margin="0,20,0,10"/>
            <DataGrid ItemsSource="{Binding RentDueInNext7Days}" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,0,0,20">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Tenant" Binding="{Binding TenantName}" Width="*"/>
                    <DataGridTextColumn Header="House" Binding="{Binding HouseAddress}" Width="*"/>
                    <DataGridTextColumn Header="Amount" Width="120">
                        <DataGridTextColumn.Binding>
                            <Binding Path="Amount" StringFormat="£{0:N2}"/>
                        </DataGridTextColumn.Binding>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Due Date" Width="120">
                        <DataGridTextColumn.Binding>
                            <Binding Path="DueDate" StringFormat="{}{0:dd/MM/yyyy}"/>
                        </DataGridTextColumn.Binding>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Overdue Rent -->
            <TextBlock Text="Overdue Rent" FontSize="20" FontWeight="Bold" Margin="0,20,0,10"/>
            <DataGrid ItemsSource="{Binding OverdueRent}" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Tenant" Binding="{Binding TenantName}" Width="*"/>
                    <DataGridTextColumn Header="House" Binding="{Binding HouseAddress}" Width="*"/>
                    <DataGridTextColumn Header="Amount" Width="120">
                        <DataGridTextColumn.Binding>
                            <Binding Path="Amount" StringFormat="£{0:N2}"/>
                        </DataGridTextColumn.Binding>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Days Late" Binding="{Binding DaysLate}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
