<UserControl x:Class="LandLordBuddy.MVVM.Views.TenantsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:LandLordBuddy.MVVM.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <UserControl.Resources>
        <converters:IsNotNullConverter x:Key="IsNotNullConverter"/>
    </UserControl.Resources>
    <StackPanel Margin="20">
        <TextBlock Text="Tenants" FontSize="28" FontWeight="Bold" Margin="0,0,0,20"/>
        
        <!-- Search -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <TextBox Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}" 
                     Width="300" Margin="0,0,10,0" 
                     Padding="10,5"/>
            <Button Content="Add Tenant" Command="{Binding AddTenantCommand}" 
                   Padding="15,8" Margin="0,0,10,0"/>
            <Button Content="Edit Tenant" Command="{Binding EditTenantCommand}" 
                   Padding="15,8" Margin="0,0,10,0"
                   Background="#2196F3"
                   Foreground="White"
                   BorderThickness="0">
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedTenant, Converter={StaticResource IsNotNullConverter}}" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                                <Setter Property="IsEnabled" Value="False"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Button Content="Remove Tenant" Command="{Binding RemoveTenantCommand}" 
                   Padding="15,8"
                   Background="#F44336"
                   Foreground="White"
                   BorderThickness="0">
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedTenant, Converter={StaticResource IsNotNullConverter}}" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                                <Setter Property="IsEnabled" Value="False"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>

        <!-- Tenants List -->
        <DataGrid ItemsSource="{Binding Tenants}" 
                 SelectedItem="{Binding SelectedTenant}"
                 AutoGenerateColumns="False" 
                 IsReadOnly="True"
                 GridLinesVisibility="All"
                 HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding FullName}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                <DataGridTextColumn Header="Phone" Binding="{Binding PhoneNumber}" Width="120"/>
                <DataGridTextColumn Header="University" Binding="{Binding UniversityName}" Width="*"/>
                <DataGridTextColumn Header="Current House" Binding="{Binding CurrentHouseAddress}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</UserControl>
