<UserControl x:Class="Daryva.MVVM.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <StackPanel Margin="0">
            <TextBlock Text="Dashboard" Style="{StaticResource Heading1}"/>
            
            <!-- KPI Cards -->
            <UniformGrid Columns="5" Margin="0,0,0,24">
                <Border Style="{StaticResource CardStyle}" Background="#1E3A5F">
                    <StackPanel>
                        <TextBlock Text="Houses" FontSize="14" Foreground="#B3D9FF"/>
                        <TextBlock Text="{Binding HousesCount}" FontSize="32" FontWeight="Bold" Foreground="White"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardStyle}" Background="#1E4A2E">
                    <StackPanel>
                        <TextBlock Text="Active Tenants" FontSize="14" Foreground="#B3FFB3"/>
                        <TextBlock Text="{Binding ActiveTenantsCount}" FontSize="32" FontWeight="Bold" Foreground="White"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardStyle}" Background="#4A3A1E">
                    <StackPanel>
                        <TextBlock Text="Rent Due This Month" FontSize="14" Foreground="#FFE6B3"/>
                        <TextBlock FontSize="32" FontWeight="Bold" Foreground="White">
                            <TextBlock.Text>
                                <Binding Path="RentDueThisMonth" StringFormat="£{0:N2}"/>
                            </TextBlock.Text>
                        </TextBlock>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardStyle}" Background="#4A1E1E">
                    <StackPanel>
                        <TextBlock Text="Overdue Rent" FontSize="14" Foreground="#FFB3B3"/>
                        <TextBlock Text="{Binding OverdueRentCount}" FontSize="32" FontWeight="Bold" Foreground="White"/>
                        <TextBlock FontSize="14" Foreground="#FFB3B3">
                            <TextBlock.Text>
                                <Binding Path="OverdueRentAmount" StringFormat="£{0:N2}"/>
                            </TextBlock.Text>
                        </TextBlock>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardStyle}" Background="#3A1E4A">
                    <StackPanel>
                        <TextBlock Text="Documents Expiring" FontSize="14" Foreground="#E6B3FF"/>
                        <TextBlock Text="{Binding DocumentsExpiringSoonCount}" FontSize="32" FontWeight="Bold" Foreground="White"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <!-- Quick Actions -->
            <TextBlock Text="Quick Actions" Style="{StaticResource Heading2}"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,24">
                <Button Content="Add House" Command="{Binding AddHouseCommand}" Style="{StaticResource PrimaryButtonStyle}"/>
                <Button Content="Add Tenant" Command="{Binding AddTenantCommand}" Style="{StaticResource PrimaryButtonStyle}"/>
                <Button Content="Record Payment" Command="{Binding RecordPaymentCommand}" Style="{StaticResource PrimaryButtonStyle}"/>
                <Button Content="Upload Document" Command="{Binding UploadDocumentCommand}" Style="{StaticResource PrimaryButtonStyle}"/>
            </StackPanel>

            <!-- Rent Due in Next 7 Days -->
            <TextBlock Text="Rent Due in Next 7 Days" Style="{StaticResource Heading2}"/>
            <DataGrid ItemsSource="{Binding RentDueInNext7Days}" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,0,0,24">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Tenant" Binding="{Binding TenantName}" Width="*"/>
                    <DataGridTextColumn Header="House" Binding="{Binding HouseAddress}" Width="*"/>
                    <DataGridTextColumn Header="Amount" Width="120">
                        <DataGridTextColumn.Binding>
                            <Binding Path="Amount" StringFormat="£{0:N2}"/>
                        </DataGridTextColumn.Binding>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Due Date" Width="120">
                        <DataGridTextColumn.Binding>
                            <Binding Path="DueDate" StringFormat="{}{0:dd/MM/yyyy}"/>
                        </DataGridTextColumn.Binding>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Overdue Rent -->
            <TextBlock Text="Overdue Rent" Style="{StaticResource Heading2}"/>
            <DataGrid ItemsSource="{Binding OverdueRent}" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Tenant" Binding="{Binding TenantName}" Width="*"/>
                    <DataGridTextColumn Header="House" Binding="{Binding HouseAddress}" Width="*"/>
                    <DataGridTextColumn Header="Amount" Width="120">
                        <DataGridTextColumn.Binding>
                            <Binding Path="Amount" StringFormat="£{0:N2}"/>
                        </DataGridTextColumn.Binding>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Days Late" Binding="{Binding DaysLate}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
</UserControl>
