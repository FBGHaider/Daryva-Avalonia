<UserControl x:Class="Daryva.MVVM.Views.RentLedgerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:Daryva.MVVM.Converters"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <UserControl.Resources>
        <converters:SubtractOneConverter x:Key="SubtractOneConverter"/>
        <converters:StatusToColorConverter x:Key="StatusToColorConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Filters -->
        <Border Grid.Row="0" Background="{DynamicResource CardFillColorDefaultBrush}" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="1" Padding="15" Margin="0,0,0,15" CornerRadius="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <!-- Month Selector -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Month:" VerticalAlignment="Center" Margin="0,0,10,5"/>
                <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" Margin="0,0,20,5">
                    <ComboBox SelectedIndex="{Binding SelectedMonth, Converter={StaticResource SubtractOneConverter}}" Width="120" Margin="0,0,10,0" Padding="5">
                        <ComboBox.Items>
                            <ComboBoxItem Content="January"/>
                            <ComboBoxItem Content="February"/>
                            <ComboBoxItem Content="March"/>
                            <ComboBoxItem Content="April"/>
                            <ComboBoxItem Content="May"/>
                            <ComboBoxItem Content="June"/>
                            <ComboBoxItem Content="July"/>
                            <ComboBoxItem Content="August"/>
                            <ComboBoxItem Content="September"/>
                            <ComboBoxItem Content="October"/>
                            <ComboBoxItem Content="November"/>
                            <ComboBoxItem Content="December"/>
                        </ComboBox.Items>
                    </ComboBox>
                    <ComboBox SelectedValue="{Binding SelectedYear}" SelectedValuePath="Content" Width="100" Padding="5">
                        <ComboBox.Items>
                            <ComboBoxItem Content="2024"/>
                            <ComboBoxItem Content="2025"/>
                            <ComboBoxItem Content="2026"/>
                            <ComboBoxItem Content="2027"/>
                        </ComboBox.Items>
                    </ComboBox>
                </StackPanel>

                <!-- House Filter -->
                <TextBlock Grid.Row="0" Grid.Column="2" Text="House:" VerticalAlignment="Center" Margin="0,0,10,5"/>
                <ComboBox Grid.Row="0" Grid.Column="3" 
                         ItemsSource="{Binding Houses}"
                         SelectedValue="{Binding SelectedHouseId}"
                         SelectedValuePath="HouseId"
                         DisplayMemberPath="AddressLine1"
                         Width="200" Margin="0,0,20,5" Padding="5"/>

                <!-- Status Filter -->
                <TextBlock Grid.Row="0" Grid.Column="4" Text="Status:" VerticalAlignment="Center" Margin="0,0,10,5"/>
                <ComboBox Grid.Row="1" Grid.Column="4" 
                         SelectedValue="{Binding StatusFilter}"
                         SelectedValuePath="Content"
                         Width="120" Padding="5">
                    <ComboBox.Items>
                        <ComboBoxItem Content="All"/>
                        <ComboBoxItem Content="Unpaid"/>
                        <ComboBoxItem Content="Part-paid"/>
                        <ComboBoxItem Content="Paid"/>
                        <ComboBoxItem Content="Overdue"/>
                    </ComboBox.Items>
                </ComboBox>

                <!-- Search -->
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Search:" VerticalAlignment="Center" Margin="0,5,10,0"/>
                <TextBox Grid.Row="1" Grid.Column="1" 
                        Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}"
                        Width="200" Margin="0,5,20,0" Padding="5"
                        ToolTip="Search by tenant name or address"/>
            </Grid>
        </Border>

        <!-- Ledger Tables - Split into Rent and Deposit -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Rent Ledger Header -->
            <TextBlock Grid.Row="0" Text="Rent Ledger" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
            
            <!-- Rent Ledger Table -->
            <DataGrid Grid.Row="1" 
                     ItemsSource="{Binding LedgerRows}"
                     SelectedItem="{Binding SelectedRow}"
                     AutoGenerateColumns="False"
                     IsReadOnly="True"
                     GridLinesVisibility="All"
                     HeadersVisibility="Column"
                     CanUserReorderColumns="False"
                     CanUserResizeColumns="True"
                     Margin="0,0,0,20"
                     MinHeight="200">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="House" Binding="{Binding HouseAddress}" Width="200"/>
                    <DataGridTextColumn Header="Tenant" Binding="{Binding TenantName}" Width="150"/>
                    <DataGridTextColumn Header="Due Date" Binding="{Binding DueDate, StringFormat='dd/MM/yyyy'}" Width="100"/>
                    <DataGridTextColumn Header="Amount Due" Binding="{Binding AmountDue, StringFormat='£{0:N2}'}" Width="100"/>
                    <DataGridTextColumn Header="Paid" Binding="{Binding AmountPaid, StringFormat='£{0:N2}'}" Width="100"/>
                    <DataGridTextColumn Header="Balance" Binding="{Binding Balance, StringFormat='£{0:N2}'}" Width="100"/>
                    <DataGridTemplateColumn Header="Status" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="{Binding Status, Converter={StaticResource StatusToColorConverter}}" 
                                       Padding="5,2" CornerRadius="3">
                                    <TextBlock Text="{Binding Status}" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Actions" Width="150">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Record Payment" 
                                           Command="{Binding DataContext.RecordPaymentCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                           CommandParameter="{Binding}"
                                           Background="{DynamicResource AccentFillColorDefaultBrush}"
                                           Foreground="{DynamicResource TextOnAccentFillColorPrimaryBrush}"
                                           BorderBrush="{DynamicResource AccentFillColorDefaultBrush}"
                                           Padding="8,4" Margin="2,0"
                                           FontSize="11"
                                           MinWidth="100"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Deposit Ledger Header -->
            <TextBlock Grid.Row="2" Text="Deposit Ledger" FontSize="18" FontWeight="Bold" Margin="0,10,0,10"/>
            
            <!-- Deposit Ledger Table -->
            <DataGrid Grid.Row="3" 
                     ItemsSource="{Binding DepositLedgerRows}"
                     AutoGenerateColumns="False"
                     IsReadOnly="True"
                     GridLinesVisibility="All"
                     HeadersVisibility="Column"
                     CanUserReorderColumns="False"
                     CanUserResizeColumns="True"
                     MinHeight="200">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="House" Binding="{Binding HouseAddress}" Width="200"/>
                    <DataGridTextColumn Header="Tenant" Binding="{Binding TenantName}" Width="150"/>
                    <DataGridTextColumn Header="Deposit Required" Binding="{Binding DepositRequired, StringFormat='£{0:N2}'}" Width="120"/>
                    <DataGridTextColumn Header="Paid" Binding="{Binding AmountPaid, StringFormat='£{0:N2}'}" Width="100"/>
                    <DataGridTextColumn Header="Balance" Binding="{Binding Balance, StringFormat='£{0:N2}'}" Width="100"/>
                    <DataGridTemplateColumn Header="Status" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="{Binding Status, Converter={StaticResource StatusToColorConverter}}" 
                                       Padding="5,2" CornerRadius="3">
                                    <TextBlock Text="{Binding Status}" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
